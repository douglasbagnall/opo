#!/bin/sh

#If this file is copied to ~/.xprofile, the following
#commands will be run.  They organise the output of the
#two screens of each video card.

xrandr --screen 0 --output DVI-0 --left-of VGA-0
xrandr --screen 1 --output DVI-1 --left-of VGA-1
